# Dockerfile for Athena Web Quiz Flask App
# TODO: Complete this file to build a Docker image for the Flask application
# 1. Use a base Python image
# 2. Set up the working directory
# 3. Copy requirements.txt and install dependencies
# 4. Copy the application code
# 5. Expose the port the app runs on
# 6. Set the command to run the app with Gunicorn for production
# Example:
# FROM python:3.9-slim
# WORKDIR /app
# COPY app/requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt
# COPY app/ .
# EXPOSE 5000
# CMD ["gunicorn", "--bind", "0.0.0.0:5000", "app:app"]
